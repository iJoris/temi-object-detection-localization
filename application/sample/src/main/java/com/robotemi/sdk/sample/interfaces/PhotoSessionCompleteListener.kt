package com.robotemi.sdk.sample.interfaces;

// Custom callback listener for photo session completion and photos taken
interface PhotoSessionCompleteListener {
    fun onPhotoSessionComplete()
    fun onPhotosTaken()
}
